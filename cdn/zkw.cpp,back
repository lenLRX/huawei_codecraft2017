#include "zkw.h"
#include <limits>
using namespace std;

bool zkw::optimize(){
	do 
		do 
		    memset(v,0,sizeof(v));
		while(aug(1,maxint));
    while(modlabel());
}

int zkw::aug(){

}

bool zkw::modlabel(){
	int d = numeric_limits<int>::max();
}